import { Metadata } from "next/types";

export const metadata: Metadata = {
    title: "Tokens",
};

const Page = () => {
    return (
        <div>
            <h2>Token</h2>
        </div>
    );
};

export default Page;
